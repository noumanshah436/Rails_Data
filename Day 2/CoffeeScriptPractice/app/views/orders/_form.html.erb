<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :credit_card_number %>
    <%= form.text_field :credit_card_number , id:"order_credit_card_number"%>
    <div id="credit_card_number_error"></div>
  </div>

  <div class="field">
    <%= form.label :credit_card_expires_on %>
    <%= form.date_select :credit_card_expires_on %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
